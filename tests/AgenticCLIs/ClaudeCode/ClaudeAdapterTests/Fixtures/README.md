# Fixtures

Synthetic (`L3`) Claude Code payloads for conformance tests. No live `claude` required.

| File | Scenario |
| --- | --- |
| `hooks/stop-with-last-message.json` | Stop hook with `last_assistant_message` |

Provenance: synthetic, 2026-06-25, redacted paths.

Live capture: run `scripts/characterize-claude-code.swift` with a logged-in `claude` install.
